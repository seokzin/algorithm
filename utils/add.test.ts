import { add } from './add';

describe('js test', () => {
  it('2 arguments', () => {
    expect(add(3, 4)).toBe(7);
  });

  it('multiple arguments', () => {
    expect(add(3, 4)).toBe(7);
  });
});
